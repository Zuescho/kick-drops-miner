# Keep container-critical files LF so they work inside the Linux image,
# regardless of the contributor's OS / core.autocrlf setting.
*.sh        text eol=lf
Dockerfile  text eol=lf
*.yml       text eol=lf
*.yaml      text eol=lf

# Binary assets
*.png binary
*.ico binary
